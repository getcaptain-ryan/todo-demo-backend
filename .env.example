# Project Configuration
PROJECT_NAME=Todo Demo Backend
VERSION=0.1.0

# Server Configuration
HOST=0.0.0.0
PORT=8001

# Database Configuration
# Format: postgresql://username:password@host:port/database_name
# Local development (docker-compose)
DATABASE_URL=postgresql://postgres:password@localhost:5433/todo_db

# Railway automatically provides DATABASE_URL for PostgreSQL service
# You don't need to set this in Railway - it's auto-generated

# PostgreSQL Configuration (for docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=todo_db

# CORS Configuration (comma-separated list)
# For development:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# For production (Railway), set in Railway dashboard:
# ALLOWED_ORIGINS=https://your-frontend.railway.app,https://www.yourdomain.com

# For both development and production:
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend.railway.app

